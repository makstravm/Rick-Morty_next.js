export enum routesUrls {
  HOME = "/",
  CHARACTERS = "/characters",
  CHARACTER = "/character",
  LOCATIONS = "/locations",
  LOCATION = "/location",
  SEASONS = "/seasons",
  EPISODE = "/seasons/episode",
  LOGIN = "/login",
}
